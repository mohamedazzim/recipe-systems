// Shared domain contracts package — SCAFFOLD §1.
// The nine views, provenance tags, and mode contracts freeze in D-05 (P0-5, wks 1–2).
// Until then this package deliberately carries no contract types: inventing them before the
// freeze would violate BUILD_PLAN §1.2 ("Contracts before generators").
export const DOMAIN_CONTRACTS_FROZEN_AT = 'P0-5 (D-05)';
