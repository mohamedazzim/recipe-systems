{
  "name": "@recipe-systems/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain contracts (nine views, provenance tags, modes) — SCAFFOLD §1. Contracts freeze in D-05 (P0-5); this package is a placeholder until then.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "jest --passWithNoTests --coverage",
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "typescript-eslint": "^8.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "@types/jest": "^29.5.0"
  }
}
