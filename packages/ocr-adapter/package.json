{
  "name": "@recipe-systems/ocr-adapter",
  "version": "0.1.0",
  "private": true,
  "description": "Provider-neutral OCR interface (Tech Stack \u00a711): recognized text, line/word info, confidence, source metadata. Confidence flows into the ERD OCR fields; low-confidence lines are flagged, never dropped (INV-04).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "jest --passWithNoTests --coverage",
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "typescript-eslint": "^8.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "@types/jest": "^29.5.0"
  }
}
