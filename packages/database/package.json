{
  "name": "@recipe-systems/database",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "Prisma — the exclusive Postgres DDL authority (SCAFFOLD §2). ERD v13: 24 canonical tables.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "jest --config jest.config.js --passWithNoTests --coverage",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@prisma/client": "^6.16.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.15.0",
    "eslint": "^9.28.0",
    "jest": "^29.7.0",
    "prisma": "^6.16.2",
    "ts-jest": "^29.3.0",
    "typescript": "^5.8.3"
  }
}
